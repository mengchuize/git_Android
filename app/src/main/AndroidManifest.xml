<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplications">
    <uses-permission android:name="android.permission.INTERNET" />
    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".classes.class2a4Activity"></activity>
        <activity android:name=".classes.class2a3Activity" />
        <activity android:name=".register.registerActivity" />
        <activity android:name=".playactivity.createActivity" />
        <activity android:name=".classes.class2a2Activity" />
        <activity android:name=".classes.class2a1Activity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:screenOrientation="portrait"/>
        <activity android:name=".myself.messageActivity" />
        <activity android:name=".myself.classActivity" />
        <activity android:name=".myself.changeActivity" />
        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".bottom.MainActivity"
            android:label="@string/app_name" />
    </application>

</manifest>